extends layout

block content
  div.jumbotron.my-3
    h1.display-4 サイト議論サイト
    p.lead サイト議論サイトは、GitHubで認証でき、議論できるサイトです！
  if user
    div
      a(href="/schedules/new").btn.btn-info サイトを投稿する
    - var hasSchedule = schedules.length > 0
    if hasSchedule
      h3.my-3 あなたの作った予定一覧
      table.table
        tr
          th 予定名
          th 更新日時
        each schedule in schedules
          tr
          td
            a(href=`/schedules/${schedule.scheduleId}`) #{schedule.scheduleName}
          td #{schedule.formattedUpdatedAt}    